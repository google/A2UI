# React Chat UI Builder Client

This client connects to the Chat UI Builder backend and incrementally renders
streamed A2UI frames.

## Run

```bash
cd samples/client/react/chat_ui_builder
npm install
npm run dev
```

By default the client talks to `http://localhost:8010`. You can override the
backend URL from the UI.
