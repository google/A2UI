name: Angular build and test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install web lib deps
        working-directory: ./renderers/web
        run: npm ci

      - name: Build web lib
        working-directory: ./renderers/web
        run: npm run build

      - name: Install Angular workspace deps
        working-directory: ./samples/client/angular
        run: npm ci

      - name: Build lib
        working-directory: ./samples/client/angular
        run: npm run build lib

      - name: Build contact sample
        working-directory: ./samples/client/angular
        run: npm run build contact

      - name: Build restaurant sample
        working-directory: ./samples/client/angular
        run: npm run build restaurant
