name: Web (Lit) build and test

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'samples/agent/adk/**'
  pull_request:
    paths-ignore:
      - 'samples/agent/adk/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Lit renderer dependencies
        working-directory: ./renderers/lit
        run: npm i

      - name: Install all workspaces' dependencies
        working-directory: ./web
        run: npm i

      - name: Build all workspaces
        working-directory: ./web
        run: npm run build --workspaces

      - name: Run all tests
        working-directory: ./web
        run: npm run test --workspace=@a2ui/web-lib
